version: 1
frontend:
  buildpath: client
  phases:
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths:
      - '.npm/**/*'
      - 'node_modules/**/*'
